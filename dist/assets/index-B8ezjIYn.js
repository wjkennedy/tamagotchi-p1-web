const root = document.getElementById("root");
if (root) {
  root.innerHTML =
    '<main style="text-align:center; padding:2rem;">' +
    '<h1>Tamagotchi P1 Emulator</h1>' +
    '<p>WASM build packaged for distribution.</p>' +
    '</main>';
}
